import React, { Component } from 'react';
import { View, Image, StyleSheet, SafeAreaView, StatusBar, ScrollView, Dimensions, Text, ImageBackground } from 'react-native';

export default class AssessmentScreen extends Component {
  constructor(props) {
    super(props);
    this.state = {

    };
  }

  render() {
    return (
      <View>
      
      </View>
    );
  }
}